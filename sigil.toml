[package]
name = "qliphoth"
version = "0.1.0"
edition = "2025"
description = "React-inspired web framework for Sigil"
authors = ["Daemoniorum LLC"]
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["rlib"]

[dependencies]
qliphoth-sys = { path = "packages/qliphoth-sys" }

[features]
default = []
gtk = []
ssr = []
