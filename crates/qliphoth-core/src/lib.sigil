//! Qliphoth Core
//!
//! Core utilities, routing, state management, and API client for the Qliphoth platform.
//! This crate provides the foundation that all other Qliphoth crates build upon.

pub scroll api;
pub scroll routes;
pub scroll state;
pub scroll config;
pub scroll utils;

// Re-exports for convenience
pub invoke api::ApiClient;
pub invoke routes::{Route, Router};
pub invoke state::{AppState, Signal, create_signal};
pub invoke config::Config;
