//! Qliphoth Core
//!
//! Core utilities, routing, state management, and API client for the Qliphoth platform.
//! This crate provides the foundation that all other Qliphoth crates build upon.

pub mod api;
pub mod routes;
pub mod state;
pub mod config;
pub mod utils;

// Re-exports for convenience
pub use api::ApiClient;
pub use routes::{Route, Router};
pub use state::{AppState, Signal, create_signal};
pub use config::Config;
