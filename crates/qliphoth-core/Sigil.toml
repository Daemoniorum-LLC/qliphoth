# Qliphoth Core - Shared utilities and infrastructure

[crate]
name = "qliphoth-core"
version = "0.1.0"
edition = "2025"
description = "Core utilities for Qliphoth platform"

[dependencies]
sigil-web = { version = "0.2.0", features = ["router", "fetch", "storage"] }
serde = { version = "1.0", features = ["derive"] }
