// Source Map Test

fn add(a: i64, b: i64) -> i64 {
    a + b
}

fn multiply(x: i64, y: i64) -> i64 {
    x * y
}

pub fn main() -> i64 {
    let sum = add(10, 20);
    let product = multiply(3, 4);
    console::log_str("Testing source maps");
    sum + product
}
