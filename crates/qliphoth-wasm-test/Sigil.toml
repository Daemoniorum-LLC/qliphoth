# Minimal WASM test crate
# Uses only bootstrap-compatible syntax

[package]
name = "qliphoth-wasm-test"
version = "0.1.0"
edition = "2025"
description = "Minimal WASM test for bootstrap compiler validation"

[lib]
crate-type = ["cdylib"]

[dependencies]
# No dependencies - pure standalone test
