[package]
name = "qliphoth-shell"
version = "0.1.0"
edition = "2025"
description = "Application-agnostic shell chrome: command palette, status bar, icon tray, panel manager"
license = "MIT OR Apache-2.0"
authors = ["Daemoniorum, LLC"]

[dependencies]
qliphoth = { path = "../../src" }

[features]
default = []
fuzzy = []     # enable CommandPalette fuzzy search (requires fuzzy crate)
