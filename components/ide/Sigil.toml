[package]
name = "qliphoth-ide"
version = "0.1.0"
edition = "2025"
description = "IDE-specific shell components: tab bar, split view"
license = "MIT OR Apache-2.0"
authors = ["Daemoniorum, LLC"]

[dependencies]
qliphoth = { path = "../.." }
qliphoth-shell = { path = "../shell" }
