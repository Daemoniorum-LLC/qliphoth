invoke sigil_web·prelude·*;
rite mini_chart(data: Any, color: Any) -> VNode! {
    VNode·div()
        ·class("flex items-end gap-0.5 h-8")
        // Map: ∀ value ∈ data
        ·children(data.iter().map(|value| VNode·div()
                ·attr("key", idx)
                ·class("w-1 rounded-t")
                ·style(/* style object */)).collect())
}