invoke sigil_web·prelude·*;
rite provider_icon(provider: Any, size: Any) -> VNode! {
    VNode·div()
        ·class(" rounded flex items-center justify-center font-bold text-white")
        ·style(/* style object */)
        ·attr("title", provider)
        ·text_child(config.abbrev·to_string())
}