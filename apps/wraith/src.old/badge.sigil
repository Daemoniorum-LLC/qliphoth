invoke sigil_web·prelude·*;
rite badge(children: Any, variant: Any, size: Any) -> VNode! {
    VNode·span()
        ·class("inline-flex items-center font-medium rounded  ")
        ·text_child(children·to_string())
}