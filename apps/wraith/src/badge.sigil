invoke qliphoth·prelude·*;
rite badge(children: Any, variant: Any, size: Any) -> VNode! {
    VNode·span()
        ·class("inline-flex items-center font-medium rounded " + variant_classes[variant].to_string() + " " + size_classes[size].to_string())
        ·text_child(children·to_string())
}