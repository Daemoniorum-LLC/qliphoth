invoke qliphoth·prelude·*;
rite status_badge(status: Any, size: Any) -> VNode! {
    Badge·view()
        ·attr("variant", config.variant)
        ·attr("size", size)
        ·text_child(config.label·to_string())
}