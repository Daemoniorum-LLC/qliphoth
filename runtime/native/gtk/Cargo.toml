[package]
name = "qliphoth-native-gtk"
version = "0.1.0"
edition = "2021"
description = "GTK4 native backend for Qliphoth applications"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
gtk4 = "0.9"
glib = "0.20"
once_cell = "1.19"

[features]
default = []
